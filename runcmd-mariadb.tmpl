runcmd:
  - echo '{{ ds.meta_data.role }}' > /etc/role
  - echo '{{ ds.meta_data.aenv }}' > /etc/aenv
  - update-grub
  - apt autoremove -y
  - touch /etc/cloud/cloud-init.disabled
  - [ mysql -u root, "USE mysql;", "UPDATE user SET password=PASSWORD('theresa1') WHERE User='root' AND Host = 'localhost';" , "FLUSH PRIVILEGES;", "exit;" ]
  - eject
  - shutdown -h now
